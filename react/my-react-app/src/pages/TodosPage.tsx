import { PagePlaceholder } from '../components/PagePlaceholder'

export function TodosPage() {
  return <PagePlaceholder title="Todos" topic="Task filtering and optimistic updates will be added in a later step." />
}
