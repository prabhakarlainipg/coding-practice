import { PagePlaceholder } from '../components/PagePlaceholder'

export function UsersPage() {
  return <PagePlaceholder title="Users" topic="User profiles and nested routes will be added in a later step." />
}
