import { PagePlaceholder } from '../components/PagePlaceholder'

export function PostsPage() {
  return <PagePlaceholder title="Posts" topic="Next, this page will consume our first typed API endpoint." />
}
